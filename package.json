{
  "name": "parcel-handlebars",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/pages/*.hbs --no-cache --public-url ./",
    "start": "npm run build && node server.js",
    "dev": "parcel src/pages/*.hbs --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "max_trufanov",
  "license": "ISC",
  "devDependencies": {
    "@anikin/parcel-transformer-handlebars": "1.0.9",
    "handlebars": "4.7.7",
    "parcel": "2.8.0"
  },
  "dependencies": {
    "dotenv": "16.0.3",
    "express": "4.18.2"
  }
}
