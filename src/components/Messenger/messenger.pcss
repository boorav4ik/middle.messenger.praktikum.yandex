.container {
  display: flex;
  width: 100%;
  /* padding: 1em; */
  margin: 2em;

  & > input {
    background-color: var(--main-grey);
    border-radius: 30px;
  }
}

.button {
  width: 40px;
  border-radius: 50%;
}